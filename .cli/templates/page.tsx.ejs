import  DynamicTable  from '@/shared/ui/DynamicTable';
import { <%= entityName %>Config } from '@/entities/<%= entityName %>/api/<%= entityName %>-api';

export function <%= entityPascalCase %>Page() {
  return (
    <div className="space-y-4">
      <DynamicTable config={<%= entityName %>Config} />
    </div>
  );
}